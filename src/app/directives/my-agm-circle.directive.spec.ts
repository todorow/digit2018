import {MyAgmCircleDirective} from './my-agm-circle.directive';

describe('MyAgmCircleDirective', () => {
  it('should create an instance', () => {
    const directive = new MyAgmCircleDirective();
    expect(directive).toBeTruthy();
  });
});
